% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-contract.R
\name{lamp_contract}
\alias{lamp_contract}
\title{The panel contract}
\usage{
lamp_contract(x)
}
\arguments{
\item{x}{A table produced by a \verb{lamp_read_*()} function or by
\code{\link[=lamp_panel]{lamp_panel()}}, or a contract.}
}
\value{
A list of class \code{lamp_contract} with elements \code{source},
\code{snapshots}, \code{versions}, \code{coverage}, \code{geography}, \code{population},
\code{crime_scope}, \code{stops}, \code{treatment} and \code{created}. Elements not yet
applicable are \code{NULL}.
}
\description{
Every table streetlamp derives from the archive carries a contract as an
attribute: the archive snapshots and versions it was built from, the
coverage status of every force-month, the LSOA vintage, the crime scope, the
stop-count definition and the treatment definition. \code{lamp_contract()}
returns it; the contract survives subsetting with \code{[} and dplyr verbs.
}
\examples{
cache <- tempfile("streetlamp-cache-")
zip <- system.file("extdata", "archive", "2026-07.zip", package = "streetlamp")
lamp_archive_register(zip, dir = cache)
crime <- lamp_read_crime(cache, forces = "dyfed-powys")
contract <- lamp_contract(crime)
contract
contract$coverage
}
\concept{contract}
