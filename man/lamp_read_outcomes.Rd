% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest-crime.R
\name{lamp_read_outcomes}
\alias{lamp_read_outcomes}
\title{Read outcome records from the archive}
\usage{
lamp_read_outcomes(
  dir = lamp_cache_dir(),
  versions = NULL,
  months = NULL,
  forces = NULL,
  fetch = FALSE
)
}
\arguments{
\item{dir}{A cache directory (see \code{\link[=lamp_cache_dir]{lamp_cache_dir()}}) or a \code{lamp_snapshot}
from \code{\link[=lamp_archive_snapshot]{lamp_archive_snapshot()}}.}

\item{versions}{A \code{lamp_selection} from \code{\link[=lamp_select_version]{lamp_select_version()}}; by default
the newest archive version of each file is used.}

\item{months, forces}{Optional filters, as in \code{\link[=lamp_archive_download]{lamp_archive_download()}}.}

\item{fetch}{Fetch selected files that are listed in the snapshot but not
held locally (needs network access). With the default \code{FALSE} only the
files held locally are read, with a message saying how many were skipped,
so a reader never starts a large download on its own.}
}
\value{
A tibble of class \code{lamp_records} with columns \code{crime_id}, \code{month},
\code{reported_by}, \code{falls_within}, \code{force_id}, \code{longitude}, \code{latitude},
\code{location}, \code{lsoa_code}, \code{lsoa_name}, \code{lsoa_vintage}, \code{outcome_type}
(factor), \code{outcome_type_raw}, \code{outcome_group} (factor with the six
groups of \code{\link[=lamp_outcome_types]{lamp_outcome_types()}}), \code{archive} and \code{file}, plus a contract.
}
\description{
Reads the selected \code{outcomes} files into one table with a fixed schema,
classifies outcomes with \code{\link[=lamp_outcome_types]{lamp_outcome_types()}}, detects the LSOA vintage
of every file, and attaches a panel contract. An outcomes file for a month
lists the outcomes recorded in that month, whose crimes may date from
earlier months, and a crime can have several outcomes.
}
\examples{
cache <- tempfile("streetlamp-cache-")
zip <- system.file("extdata", "archive", "2026-07.zip", package = "streetlamp")
lamp_archive_register(zip, dir = cache)
outcomes <- lamp_read_outcomes(cache, forces = "dyfed-powys", months = "2026-07")
table(outcomes$outcome_group)
}
\seealso{
Other ingest: 
\code{\link{lamp_archive_download}()},
\code{\link{lamp_archive_index}()},
\code{\link{lamp_archive_register}()},
\code{\link{lamp_archive_snapshot}()},
\code{\link{lamp_list_versions}()},
\code{\link{lamp_lsoa_vintage}()},
\code{\link{lamp_read_crime}()},
\code{\link{lamp_read_stop_counts}()},
\code{\link{lamp_s60_legislation}()},
\code{\link{lamp_select_version}()},
\code{\link{lamp_version_diff}()}
}
\concept{ingest}
