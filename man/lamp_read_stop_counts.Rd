% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest-stops.R
\name{lamp_read_stop_counts}
\alias{lamp_read_stop_counts}
\title{Count stops by area and month}
\usage{
lamp_read_stop_counts(
  dir = lamp_cache_dir(),
  versions = NULL,
  area = "force",
  boundaries = NULL,
  months = NULL,
  forces = NULL,
  fetch = FALSE
)
}
\arguments{
\item{dir}{A cache directory (see \code{\link[=lamp_cache_dir]{lamp_cache_dir()}}) or a \code{lamp_snapshot}
from \code{\link[=lamp_archive_snapshot]{lamp_archive_snapshot()}}.}

\item{versions}{A \code{lamp_selection} from \code{\link[=lamp_select_version]{lamp_select_version()}}; by default
the newest archive version of each file is used.}

\item{area}{\code{"force"} (default) for force-month counts, or \code{"lsoa21"},
\code{"lsoa11"} or another label describing the polygons in \code{boundaries}.}

\item{boundaries}{An \code{sf} polygon layer with an \code{area} column giving the
area identifier; required unless \code{area = "force"}. \code{lamp_boundaries()}
provides ONS LSOA boundaries.}

\item{months, forces}{Optional filters, as in \code{\link[=lamp_archive_download]{lamp_archive_download()}}.}

\item{fetch}{Fetch selected files that are listed in the snapshot but not
held locally (needs network access). With the default \code{FALSE} only the
files held locally are read, with a message saying how many were skipped,
so a reader never starts a large download on its own.}
}
\value{
A tibble of class \code{lamp_records} with one row per area and month:
\code{area}, \code{month}, \code{force_id} (the submitting force), \code{stops},
\code{stops_s60} and \code{archive}; for \code{area = "force"} also \code{stops_no_location}
(searches without coordinates, which cannot be placed in an area) and
\code{stops_no_legislation}. The contract's \code{stops} element records the
origin (\code{streetlamp}), the definition (\verb{raw count}), the area type and
per-file diagnostics, and its \code{coverage} element marks force-months with
no stop-and-search file as \code{missing}.
}
\description{
Reads the selected \code{stop-and-search} files and counts searches by area and
month, with a Section 60 flag. Only the \code{Date}, \code{Latitude}, \code{Longitude} and
\code{Legislation} columns are read; the package never parses the ethnicity,
object, outcome or other fields. Area assignment is by point-in-polygon in
British National Grid (EPSG:27700) against boundaries you supply; without
boundaries, counts are per force (the force that submitted the file).
Stops are dated by the month of the file they were published in: the
publisher stores local time as UTC, so a handful of searches made after
23:00 on the first night of a summer-time month carry the previous month's
date, and the count of such rows is reported per file as
\code{n_outside_month}.
}
\examples{
cache <- tempfile("streetlamp-cache-")
zip <- system.file("extdata", "archive", "2026-07.zip", package = "streetlamp")
lamp_archive_register(zip, dir = cache)
stops <- lamp_read_stop_counts(cache, forces = c("west-yorkshire", "dyfed-powys"))
stops
lamp_contract(stops)$coverage
}
\seealso{
Other ingest: 
\code{\link{lamp_archive_download}()},
\code{\link{lamp_archive_index}()},
\code{\link{lamp_archive_register}()},
\code{\link{lamp_archive_snapshot}()},
\code{\link{lamp_list_versions}()},
\code{\link{lamp_lsoa_vintage}()},
\code{\link{lamp_read_crime}()},
\code{\link{lamp_read_outcomes}()},
\code{\link{lamp_s60_legislation}()},
\code{\link{lamp_select_version}()},
\code{\link{lamp_version_diff}()}
}
\concept{ingest}
