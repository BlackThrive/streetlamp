% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/est-costbenefit.R
\name{lamp_crimes_prevented}
\alias{lamp_crimes_prevented}
\title{Convert an estimate into crimes prevented per thousand searches}
\usage{
lamp_crimes_prevented(
  estimate,
  panel,
  per_stops = 1000,
  stops_added = NULL,
  outcome_column = NULL,
  n_boot = 2000,
  seed = NULL
)
}
\arguments{
\item{estimate}{A \code{lamp_estimate} from \code{\link[=lamp_elasticity]{lamp_elasticity()}},
\code{\link[=lamp_twfe]{lamp_twfe()}}, \code{\link[=lamp_did_staggered]{lamp_did_staggered()}} or \code{\link[=lamp_event_study]{lamp_event_study()}}.}

\item{panel}{The panel the estimate was built from, for the sample means.}

\item{per_stops}{Searches to express the result per, default 1000.}

\item{stops_added}{For a treatment effect: the searches the intervention
added per area-month. Required unless the estimate is an elasticity.}

\item{outcome_column}{The crime column to take the mean of; defaults to the
estimate's outcome.}

\item{n_boot}{Bootstrap replications for the interval, default 2000.}

\item{seed}{Random seed.}
}
\value{
A list of class \code{lamp_crimes_prevented} with \code{crimes_prevented},
\code{conf_low}, \code{conf_high}, the inputs used, and \code{assumptions}, a character
vector naming every step from the estimate to the number.
}
\description{
Takes an elasticity or a treatment effect and expresses it as the number of
crimes prevented per thousand searches at the sample mean, which is the
form in which the question is usually asked. The conversion is arithmetic,
not evidence: it inherits every assumption of the estimate it is given, and
adds more of its own. The assumption chain is returned with the number and
printed with it.
}
\details{
For an elasticity \code{e}, a proportional change in searches \code{dS/S} changes
crime by \code{e * dS/S}, so at mean monthly crime \code{C} and mean searches \code{S} the
crimes prevented by an extra \code{per_stops} searches are
\code{-e * C * per_stops / S}. For a treatment effect in log points \code{b}, the
effect on crime is \code{C * (exp(b) - 1)}, and it is divided by the searches
the intervention actually added, which you must supply as \code{stops_added}
because a binary treatment does not say how much searching it involved.
}
\section{What this number does not include}{

It counts recorded crime only, so crimes not reported to the police are
invisible to it, and any change in recording practice is counted as a
change in crime. It assumes the effect is linear in searches over the
range considered, and that the average effect applies at the margin, which
is the opposite of what diminishing returns implies. It says nothing about
the costs of searching, which fall on the people searched.
}

\examples{
sim <- lamp_simulate(n_areas = 30, n_months = 30, design = "continuous", seed = 1)
el <- lamp_elasticity(sim, "crime_total", lags = 0:1)
lamp_crimes_prevented(el, sim)
}
\seealso{
Other estimators: 
\code{\link{lamp_allocation}()},
\code{\link{lamp_did_staggered}()},
\code{\link{lamp_displacement_quotient}()},
\code{\link{lamp_elasticity}()},
\code{\link{lamp_event_study}()},
\code{\link{lamp_spillover}()},
\code{\link{lamp_synth}()},
\code{\link{lamp_twfe}()},
\code{\link{tidy.lamp_estimate}()}
}
\concept{estimators}
