% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/est-spillover.R
\name{lamp_displacement_quotient}
\alias{lamp_displacement_quotient}
\title{Weighted displacement quotient}
\usage{
lamp_displacement_quotient(
  panel,
  outcome = "crime_total",
  treated_areas,
  buffer_areas,
  control_areas,
  pre,
  post,
  n_boot = 1000,
  seed = NULL
)
}
\arguments{
\item{panel}{A \code{lamp_panel}.}

\item{outcome}{The outcome column.}

\item{treated_areas, buffer_areas, control_areas}{Area identifiers.}

\item{pre, post}{Two-element vectors giving the first and last month of the
before and after periods.}

\item{n_boot}{Bootstrap replications over areas, default 1000.}

\item{seed}{Random seed.}
}
\value{
A list of class \code{lamp_wdq} with \code{wdq}, \code{conf_low}, \code{conf_high},
the underlying totals, \code{success} (the treated area's change net of
controls) and \code{interpretation}.
}
\description{
Measures whether crime prevented in a treated area reappeared in the areas
around it. The quotient compares the change in a buffer of surrounding
areas with the change in control areas, relative to the change in the
treated areas, following Bowers and Johnson (2003).
}
\details{
The statistic is
\code{WDQ = ((B1/C1) - (B0/C0)) / ((A1/C1) - (A0/C0))}, where \code{A}, \code{B} and \code{C}
are total crime in the treated, buffer and control areas and the
subscripts are the pre and post periods. Read it as:
\itemize{
\item negative: crime moved into the buffer, so the gain in the treated area is
partly or wholly displacement;
\item between 0 and 1: the buffer improved too, a diffusion of benefit smaller
than the treated area's gain;
\item above 1: the buffer improved more than the treated area.
}

The quotient is a descriptive ratio, not a causal estimate: it assumes the
control areas show what would have happened everywhere, and it is unstable
when the treated-area change is close to zero, which the bootstrap
interval will show as a very wide range.
}
\examples{
sim <- lamp_simulate(
  n_areas = 36, n_months = 20, design = "spillover",
  effect = -0.4, spillover_effect = 0.2, seed = 2
)
truth <- attr(sim, "truth")
areas <- truth$lattice$area
lamp_displacement_quotient(
  sim, "crime_total",
  treated_areas = truth$treated_areas[1:6],
  buffer_areas = truth$treated_areas[7:12],
  control_areas = setdiff(areas, truth$treated_areas),
  pre = c("2018-01", "2018-06"), post = c("2018-12", "2019-05"), n_boot = 50
)
}
\references{
Bowers, K. J. and Johnson, S. D. (2003). Measuring the
geographical displacement and diffusion of benefit effects of crime
prevention activity. Journal of Quantitative Criminology 19(3), 275-301.
}
\seealso{
Other estimators: 
\code{\link{lamp_allocation}()},
\code{\link{lamp_crimes_prevented}()},
\code{\link{lamp_did_staggered}()},
\code{\link{lamp_elasticity}()},
\code{\link{lamp_event_study}()},
\code{\link{lamp_spillover}()},
\code{\link{lamp_synth}()},
\code{\link{lamp_twfe}()},
\code{\link{tidy.lamp_estimate}()}
}
\concept{estimators}
