% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest-crime.R
\name{lamp_read_crime}
\alias{lamp_read_crime}
\title{Read street-level crime records from the archive}
\usage{
lamp_read_crime(
  dir = lamp_cache_dir(),
  versions = NULL,
  months = NULL,
  forces = NULL,
  fetch = FALSE
)
}
\arguments{
\item{dir}{A cache directory (see \code{\link[=lamp_cache_dir]{lamp_cache_dir()}}) or a \code{lamp_snapshot}
from \code{\link[=lamp_archive_snapshot]{lamp_archive_snapshot()}}.}

\item{versions}{A \code{lamp_selection} from \code{\link[=lamp_select_version]{lamp_select_version()}}; by default
the newest archive version of each file is used.}

\item{months, forces}{Optional filters, as in \code{\link[=lamp_archive_download]{lamp_archive_download()}}.}

\item{fetch}{Fetch selected files that are listed in the snapshot but not
held locally (needs network access). With the default \code{FALSE} only the
files held locally are read, with a message saying how many were skipped,
so a reader never starts a large download on its own.}
}
\value{
A tibble of class \code{lamp_records} with columns \code{crime_id}, \code{month}
(Date, first of month), \code{reported_by}, \code{falls_within}, \code{force_id} (from
the file name), \code{longitude}, \code{latitude}, \code{location}, \code{lsoa_code},
\code{lsoa_name}, \code{lsoa_vintage}, \code{crime_type} (factor), \code{crime_type_raw},
\code{is_asb}, \code{last_outcome_category}, \code{context}, \code{archive} and \code{file}, and
a contract retrievable with \code{\link[=lamp_contract]{lamp_contract()}} whose \code{coverage} element
marks force-months with no file as \code{missing}.
}
\description{
Reads the selected \code{street} files into one table with a fixed snake_case
schema, harmonises crime type labels across the three category sets that
data.police.uk has used since December 2010, flags anti-social behaviour,
detects the LSOA vintage of every file, and attaches a panel contract.
}
\details{
Crime types are returned as a factor whose levels are the fourteen current
categories plus \verb{Public disorder and weapons}, the legacy category that was
split in May 2013 and cannot be mapped forward. \verb{Violent crime}, the
pre-2013 name of \verb{Violence and sexual offences}, is renamed. The raw label
is kept in \code{crime_type_raw}, and the contract's \code{crime_scope$category_sets}
records which set each file uses (\code{six} for December 2010 to August 2011,
\code{eleven} to April 2013, \code{fourteen} since May 2013).

Anti-social behaviour rows have no Crime ID and no outcome; they are kept
with \code{is_asb = TRUE} so that \code{lamp_panel()} can hold them in a separate
series. Rows without coordinates or LSOA code are kept as \code{NA}.
}
\examples{
cache <- tempfile("streetlamp-cache-")
zip <- system.file("extdata", "archive", "2026-07.zip", package = "streetlamp")
lamp_archive_register(zip, dir = cache)
crime <- lamp_read_crime(cache, forces = "dyfed-powys")
crime
table(crime$crime_type)
lamp_contract(crime)$coverage
}
\seealso{
Other ingest: 
\code{\link{lamp_archive_download}()},
\code{\link{lamp_archive_index}()},
\code{\link{lamp_archive_register}()},
\code{\link{lamp_archive_snapshot}()},
\code{\link{lamp_list_versions}()},
\code{\link{lamp_lsoa_vintage}()},
\code{\link{lamp_read_outcomes}()},
\code{\link{lamp_read_stop_counts}()},
\code{\link{lamp_s60_legislation}()},
\code{\link{lamp_select_version}()},
\code{\link{lamp_version_diff}()}
}
\concept{ingest}
