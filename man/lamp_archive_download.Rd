% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest-archive.R
\name{lamp_archive_download}
\alias{lamp_archive_download}
\title{Download force-month files from the data.police.uk archive}
\usage{
lamp_archive_download(
  archives = "latest",
  months = NULL,
  forces = NULL,
  file_types = c("street", "outcomes", "stop-and-search"),
  dir = lamp_cache_dir(),
  index = NULL,
  relist = FALSE
)
}
\arguments{
\item{archives}{Archive identifiers such as \code{c("2026-06", "2026-07")}, or
\code{"latest"} (the default) for the newest snapshot in \code{\link[=lamp_archive_index]{lamp_archive_index()}}.}

\item{months}{Months to fetch as \code{"YYYY-MM"} strings or Dates; \code{NULL} for
every month in the archive.}

\item{forces}{police.uk force identifiers such as \code{"west-yorkshire"};
\code{NULL} for every force. See \code{\link[=lamp_forces]{lamp_forces()}}.}

\item{file_types}{Which of \code{"street"}, \code{"outcomes"} and
\code{"stop-and-search"} to fetch.}

\item{dir}{Cache directory; see \code{\link[=lamp_cache_dir]{lamp_cache_dir()}}.}

\item{index}{An archive index from \code{\link[=lamp_archive_index]{lamp_archive_index()}}, to avoid fetching
it again.}

\item{relist}{Read the archive's table of contents again even if it has been
listed before.}
}
\value{
A \code{lamp_snapshot} (see \code{\link[=lamp_archive_snapshot]{lamp_archive_snapshot()}}) for \code{dir},
invisibly.
}
\description{
Fetches the street, outcomes and stop-and-search CSV files for chosen
months and forces from one or more archive snapshots, without downloading
the whole zip: the zip's table of contents is read with an HTTP byte-range
request and each wanted file is then fetched by range and inflated locally.
Files already present in the cache with a matching checksum are skipped.
Each fetched file is recorded with its CRC32 from the archive, a SHA-256 of
its content and the time of download, which is what the panel contract
reports as provenance.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
# Requires network access; fetches about 20 MB
snap <- lamp_archive_download(
  archives = "latest", months = c("2026-05", "2026-06", "2026-07"),
  forces = c("west-yorkshire", "dyfed-powys")
)
snap
\dontshow{\}) # examplesIf}
}
\seealso{
Other ingest: 
\code{\link{lamp_archive_index}()},
\code{\link{lamp_archive_register}()},
\code{\link{lamp_archive_snapshot}()},
\code{\link{lamp_list_versions}()},
\code{\link{lamp_lsoa_vintage}()},
\code{\link{lamp_read_crime}()},
\code{\link{lamp_read_outcomes}()},
\code{\link{lamp_read_stop_counts}()},
\code{\link{lamp_s60_legislation}()},
\code{\link{lamp_select_version}()},
\code{\link{lamp_version_diff}()}
}
\concept{ingest}
