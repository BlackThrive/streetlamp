% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest-versions.R
\name{lamp_list_versions}
\alias{lamp_list_versions}
\title{List the archive versions of every force-month file}
\usage{
lamp_list_versions(snapshot, file_types = NULL)
}
\arguments{
\item{snapshot}{A \code{lamp_snapshot} from \code{\link[=lamp_archive_snapshot]{lamp_archive_snapshot()}}.}

\item{file_types}{Restrict to some of \code{"street"}, \code{"outcomes"} and
\code{"stop-and-search"}; \code{NULL} for all.}
}
\value{
A tibble of class \code{lamp_versions} with one row per force, month and
file type: \code{n_versions}, \code{archives} (list-column of archive identifiers,
oldest first), \code{crc32} (list-column, aligned with \code{archives}), \code{differs}
(\code{TRUE} when the versions do not all share one checksum), \code{usize_min},
\code{usize_max}, and \code{n_available} (versions readable locally).
}
\description{
List the archive versions of every force-month file
}
\examples{
cache <- tempfile("streetlamp-cache-")
zips <- list.files(
  system.file("extdata", "archive", package = "streetlamp"),
  pattern = "zip$", full.names = TRUE
)
for (z in zips) lamp_archive_register(z, dir = cache)
v <- lamp_list_versions(lamp_archive_snapshot(cache))
v[v$n_versions > 1, c("force_id", "month", "file_type", "n_versions", "differs")]
}
\seealso{
Other ingest: 
\code{\link{lamp_archive_download}()},
\code{\link{lamp_archive_index}()},
\code{\link{lamp_archive_register}()},
\code{\link{lamp_archive_snapshot}()},
\code{\link{lamp_lsoa_vintage}()},
\code{\link{lamp_read_crime}()},
\code{\link{lamp_read_outcomes}()},
\code{\link{lamp_read_stop_counts}()},
\code{\link{lamp_s60_legislation}()},
\code{\link{lamp_select_version}()},
\code{\link{lamp_version_diff}()}
}
\concept{ingest}
