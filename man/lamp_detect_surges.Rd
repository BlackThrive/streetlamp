% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/treatment-define.R
\name{lamp_detect_surges}
\alias{lamp_detect_surges}
\title{Detect surges in stop and search activity}
\usage{
lamp_detect_surges(
  panel,
  method = c("threshold", "breakpoints"),
  k = 2,
  min_months = 12L,
  min_stops = 3L
)
}
\arguments{
\item{panel}{A \code{lamp_panel} with a \code{stops} column.}

\item{method}{\code{"threshold"} (default) or \code{"breakpoints"}.}

\item{k}{Threshold in standard deviations above the trailing mean.}

\item{min_months}{Months of history required before an area can be
flagged.}

\item{min_stops}{Minimum searches in the month, so that tiny areas do not
surge from one search to three.}
}
\value{
A tibble of class \code{lamp_surges} with one row per area:
\code{area}, \code{adoption_month} (the first flagged month, \code{NA} if none),
\code{n_flagged}, \code{trailing_mean}, \code{trailing_sd}, \code{peak_stops} and
\code{peak_ratio} (peak over trailing mean). Pass it to
\code{\link[=lamp_treatment]{lamp_treatment()}} as \code{adoption} for a staggered design. The attribute
\code{flags} holds every flagged area-month.
}
\description{
Flags area-months in which searches rise far above the area's recent level,
and returns them as a candidate staggered-adoption table. Two methods are
offered: a threshold of \code{k} standard deviations above the area's trailing
twelve-month mean, and a structural break test from \code{strucchange}.
}
\section{Endogeneity}{

A surge detected from the stop series is not an experiment. Police deploy
searches where crime has risen, so an area's first surge month is
correlated with its recent crime history and with anything else that
prompted the deployment. Treating a detected surge as exogenous will
mistake the response for the cause. Use this to explore where activity
changed and to pick candidate cases for a design with an external source
of variation, not as the primary design; and read
\code{lamp_allocation()} alongside any estimate that uses it.
}

\examples{
sim <- lamp_simulate(n_areas = 16, n_months = 30, design = "event", seed = 3)
surges <- lamp_detect_surges(sim, min_stops = 5)
head(surges)
}
\seealso{
Other treatment: 
\code{\link{lamp_treatment}()}
}
\concept{treatment}
