% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/treatment-define.R
\name{lamp_treatment}
\alias{lamp_treatment}
\title{Define the treatment}
\usage{
lamp_treatment(
  panel,
  type = c("continuous", "binary", "staggered", "event"),
  measure = c("stop_rate", "stops", "stops_per_100_crimes"),
  transform = c("identity", "log", "ihs"),
  areas = NULL,
  window = NULL,
  adoption = NULL,
  event = NULL,
  date = NULL,
  scope = NULL
)
}
\arguments{
\item{panel}{A \code{lamp_panel} from \code{\link[=lamp_panel]{lamp_panel()}}.}

\item{type}{\code{"continuous"}, \code{"binary"}, \code{"staggered"} or \code{"event"}.}

\item{measure, transform}{For \code{continuous}: which intensity and which
transformation.}

\item{areas}{For \code{binary}: the treated areas.}

\item{window}{For \code{binary}: the first and last treated month. For \code{event}:
the event window in months, default \code{c(-12, 12)}.}

\item{adoption}{For \code{staggered}: a table with columns \code{area} and
\code{adoption_month}.}

\item{event}{For \code{event}: a name in \code{\link[=lamp_shocks]{lamp_shocks()}}, or \code{NULL} when \code{date}
is given.}

\item{date}{For \code{event}: the event month, overriding the shocks table.}

\item{scope}{For \code{event}: areas the event applies to; \code{NULL} for all.}
}
\value{
A \code{lamp_treatment} object: a list with \code{type}, \code{column} (the
treatment variable's name), \code{data} (area, month, the treatment column
and, where applicable, \code{rel_time} and \code{cohort}), \code{details},
\code{n_treated_areas}, \code{n_control_areas} and \code{created}.
}
\description{
Turns a panel into a treatment definition that the estimators can use: a
continuous stop intensity, a binary intervention in named areas and months,
staggered adoption dates by area, or a dated event applied to every area.
The result records how the treatment was built, is stored in the contract
of every estimate made with it, and is printed by the estimators alongside
their identifying assumption.
}
\details{
Types:
\itemize{
\item \code{continuous}: \code{measure} is \code{"stops"}, \code{"stop_rate"} (per 1,000
residents, the default) or \code{"stops_per_100_crimes"} (searches per 100
crimes recorded in the area's previous twelve months, which removes the
mechanical dependence on population). \code{transform} is \code{"identity"},
\code{"log"} (log of one plus the measure) or \code{"ihs"} (inverse hyperbolic
sine, which handles zeros).
\item \code{binary}: \code{areas} are treated during \code{window}, a pair of months, and
untreated outside it; every other area is a control.
\item \code{staggered}: \code{adoption} is a table of \code{area} and \code{adoption_month}. Areas
absent from it are never treated. \code{rel_time} (months since adoption) and
\code{cohort} are added.
\item \code{event}: a dated shock applied to every area in scope, from
\code{\link[=lamp_shocks]{lamp_shocks()}} by \code{event} name or from \code{date}, with an event \code{window}
in months around it. \code{rel_time} is months since the event.
}

A continuous treatment is a dose, not an experiment: the allocation of
searches to areas responds to crime, so estimates from it describe an
association unless the variation is argued to be exogenous. See
\code{lamp_allocation()}.
}
\examples{
sim <- lamp_simulate(n_areas = 16, n_months = 18, design = "event", seed = 1)

# a dated event, twelve months either side
lamp_treatment(sim, "event", date = "2018-10", window = c(-6, 6))

# stop intensity as a continuous dose
lamp_treatment(sim, "continuous", measure = "stop_rate", transform = "ihs")

# a surge in named areas
lamp_treatment(sim, "binary", areas = unique(sim$area)[1:4], window = c("2018-06", "2018-12"))
}
\seealso{
Other treatment: 
\code{\link{lamp_detect_surges}()}
}
\concept{treatment}
