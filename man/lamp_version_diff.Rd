% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest-versions.R
\name{lamp_version_diff}
\alias{lamp_version_diff}
\title{Compare the archive versions of one force-month file}
\usage{
lamp_version_diff(
  snapshot,
  force,
  month,
  file_type = c("street", "outcomes", "stop-and-search"),
  fetch = FALSE
)
}
\arguments{
\item{snapshot}{A \code{lamp_snapshot} from \code{\link[=lamp_archive_snapshot]{lamp_archive_snapshot()}}.}

\item{force}{A police.uk force identifier.}

\item{month}{A month as \code{"YYYY-MM"} or a Date.}

\item{file_type}{One of \code{"street"}, \code{"outcomes"}, \code{"stop-and-search"}.}

\item{fetch}{Fetch versions that are listed but not held locally (needs
network access).}
}
\value{
A tibble with one row per version: \code{archive}, \code{n_rows}, \code{n_ids}
(distinct crime identifiers; \code{NA} for stop-and-search files),
\code{ids_not_in_others} (identifiers absent from every other version) and
\code{crc32}. The attribute \code{outcome_changes} gives, for street files, the
number of shared crimes whose last outcome category differs between the
oldest and newest versions.
}
\description{
Reads every available version of a file and reports what differs: the
number of rows, and for street and outcomes files the crime identifiers
present in one version but not another. Street files routinely differ
between snapshots only in their \verb{Last outcome category} column, which is
why row and identifier counts, not checksums, decide whether a force
re-supplied a month.
}
\examples{
cache <- tempfile("streetlamp-cache-")
zips <- list.files(
  system.file("extdata", "archive", package = "streetlamp"),
  pattern = "zip$", full.names = TRUE
)
for (z in zips) lamp_archive_register(z, dir = cache)
snap <- lamp_archive_snapshot(cache)
lamp_version_diff(snap, "west-yorkshire", "2026-05", "outcomes")
}
\seealso{
Other ingest: 
\code{\link{lamp_archive_download}()},
\code{\link{lamp_archive_index}()},
\code{\link{lamp_archive_register}()},
\code{\link{lamp_archive_snapshot}()},
\code{\link{lamp_list_versions}()},
\code{\link{lamp_lsoa_vintage}()},
\code{\link{lamp_read_crime}()},
\code{\link{lamp_read_outcomes}()},
\code{\link{lamp_read_stop_counts}()},
\code{\link{lamp_s60_legislation}()},
\code{\link{lamp_select_version}()}
}
\concept{ingest}
