% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{lamp_table}
\alias{lamp_table}
\alias{lamp_table.lamp_estimate}
\alias{lamp_table.lamp_event_study}
\alias{lamp_table.lamp_did_staggered}
\alias{lamp_table.lamp_pretrends}
\alias{lamp_table.lamp_synth}
\alias{lamp_table.lamp_coverage}
\title{A presentation-ready table from a streetlamp object}
\usage{
lamp_table(x, ...)

\method{lamp_table}{lamp_estimate}(x, digits = 3, ...)

\method{lamp_table}{lamp_event_study}(x, digits = 3, ...)

\method{lamp_table}{lamp_did_staggered}(x, digits = 3, ...)

\method{lamp_table}{lamp_pretrends}(x, digits = 3, ...)

\method{lamp_table}{lamp_synth}(x, digits = 3, ...)

\method{lamp_table}{lamp_coverage}(x, ...)
}
\arguments{
\item{x}{An estimate from any of the package's estimators, a
\code{\link[=lamp_pretrends]{lamp_pretrends()}} result, a \code{\link[=lamp_synth]{lamp_synth()}} fit, or a \code{\link[=lamp_coverage]{lamp_coverage()}}
audit.}

\item{...}{Unused.}

\item{digits}{Significant figures for estimates and intervals.}
}
\value{
A tibble of character columns. For estimates: the term, the
estimate, its standard error, the 95 percent confidence interval and the
p value. Event studies and staggered fits show months relative to the
event in place of the term. For a pre-trend diagnostic: the power table.
For a synthetic control: donor weights. For a coverage audit: counts of
force-months by file type and status.
}
\description{
Formats an estimate, a diagnostic or a coverage audit as a tibble of
character columns with readable headers, rounded figures, confidence
intervals in brackets and p values as \verb{<0.001} where they are that small.
The result is meant to be passed to \code{knitr::kable()} or a similar table
renderer; the unrounded numbers remain available from \code{\link[=tidy]{tidy()}} and from
the object itself.
}
\examples{
sim <- lamp_simulate(n_areas = 24, n_months = 24, design = "event", effect = -0.2, seed = 2)
truth <- attr(sim, "truth")
tr <- lamp_treatment(sim, "event", date = truth$event_date, scope = truth$treated_areas)
lamp_table(lamp_twfe(sim, "crime_total", tr))
lamp_table(lamp_coverage(sim))
}
\seealso{
Other presentation: 
\code{\link{lamp_colours}()},
\code{\link{lamp_theme}()}
}
\concept{presentation}
