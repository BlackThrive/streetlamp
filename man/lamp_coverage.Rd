% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit-coverage.R
\name{lamp_coverage}
\alias{lamp_coverage}
\alias{lamp_coverage_compare}
\title{Coverage audit: which force-months exist, and can be compared}
\usage{
lamp_coverage(x, changelog = NULL)

lamp_coverage_compare(coverage, period_a, period_b, file_type = "street")
}
\arguments{
\item{x}{A \code{lamp_panel} from \code{\link[=lamp_panel]{lamp_panel()}} or records from
\code{\link[=lamp_read_crime]{lamp_read_crime()}}, \code{\link[=lamp_read_outcomes]{lamp_read_outcomes()}} or
\code{\link[=lamp_read_stop_counts]{lamp_read_stop_counts()}}.}

\item{changelog}{Optional \code{\link[=lamp_changelog]{lamp_changelog()}} table; entries saying a force
re-supplied months mark those months \code{refreshed}.}

\item{coverage}{A \code{lamp_coverage} table.}

\item{period_a, period_b}{Two periods, each a length-two vector of months
(\code{"YYYY-MM"} or Dates) giving the first and last month inclusive.}

\item{file_type}{Which file type to compare (default \code{"street"}).}
}
\value{
\code{lamp_coverage()} returns a tibble of class \code{lamp_coverage} with
columns \code{force_id}, \code{month}, \code{file_type}, \code{status}, \code{n_records},
\code{archive}, \code{n_versions}, \code{versions_differ}, \code{note} and \code{mismatch}.

\code{lamp_coverage_compare()} returns a tibble with one row per force:
\code{n_months_a}, \code{n_usable_a}, \code{n_months_b}, \code{n_usable_b} (months with
status \code{submitted} or \code{refreshed}), \code{n_partial_a}, \code{n_partial_b},
\code{n_refreshed_a}, \code{n_refreshed_b}, \code{n_mismatch_a}, \code{n_mismatch_b} and
\code{comparable} (\code{TRUE} when every month in both periods is \code{submitted},
with no mismatch).
}
\description{
\code{lamp_coverage()} returns the force by month by file-type grid behind a
panel or a records table, with one status per cell (\code{submitted},
\code{missing}, \code{not_read}, \code{refreshed}, \code{partial_suspected}), the record count,
the archive version used, any changelog note, and a \code{mismatch} flag for
months where a stop-and-search file exists without a street file or the
reverse. Its \code{plot()} method draws the grid as a heat map.
}
\details{
\code{lamp_coverage_compare()} summarises comparability per force between two
periods: the share of months with usable files, and whether every month
in both periods was submitted without refreshes, partial submissions or
mismatches.
}
\examples{
panel <- lamp_sample_panel()
cov <- lamp_coverage(panel)
cov
table(cov$file_type, cov$status)
# Dyfed-Powys stopped submitting stop-and-search files in December 2025
cov[cov$mismatch \%in\% TRUE, c("force_id", "month", "file_type", "status")]
lamp_coverage_compare(cov, c("2024-08", "2025-07"), c("2025-08", "2026-07"),
  file_type = "stop-and-search"
)
}
\seealso{
Other panel: 
\code{\link{lamp_adjacency}()},
\code{\link{lamp_adjacency_from_nb}()},
\code{\link{lamp_boundaries}()},
\code{\link{lamp_changelog}()},
\code{\link{lamp_panel}()},
\code{\link{lamp_population}()},
\code{\link{lamp_revintage}()}
}
\concept{panel}
