% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest-versions.R
\name{lamp_select_version}
\alias{lamp_select_version}
\title{Select one archive version per force-month file}
\usage{
lamp_select_version(versions, rule = c("latest", "earliest"), prefer = NULL)
}
\arguments{
\item{versions}{Output of \code{\link[=lamp_list_versions]{lamp_list_versions()}}.}

\item{rule}{\code{"latest"} (default) takes the newest archive snapshot holding
the file, on the grounds that later snapshots carry the most complete
outcome information and any re-supplied data; \code{"earliest"} takes the
oldest, which is the version closest to first publication.}

\item{prefer}{Optional archive identifiers to use whenever they hold the
file, before applying \code{rule} to the rest.}
}
\value{
A tibble of class \code{lamp_selection} with one row per force, month
and file type: the chosen \code{archive}, its \code{crc32} and \code{usize},
\code{n_versions}, \code{differs}, \code{alternatives} (list-column of tibbles naming
the other archives with their checksums and sizes) and \code{rule}.
}
\description{
Select one archive version per force-month file
}
\examples{
cache <- tempfile("streetlamp-cache-")
zips <- list.files(
  system.file("extdata", "archive", package = "streetlamp"),
  pattern = "zip$", full.names = TRUE
)
for (z in zips) lamp_archive_register(z, dir = cache)
v <- lamp_list_versions(lamp_archive_snapshot(cache))
sel <- lamp_select_version(v, rule = "latest")
sel[, c("force_id", "month", "file_type", "archive", "n_versions")]
}
\seealso{
Other ingest: 
\code{\link{lamp_archive_download}()},
\code{\link{lamp_archive_index}()},
\code{\link{lamp_archive_register}()},
\code{\link{lamp_archive_snapshot}()},
\code{\link{lamp_list_versions}()},
\code{\link{lamp_lsoa_vintage}()},
\code{\link{lamp_read_crime}()},
\code{\link{lamp_read_outcomes}()},
\code{\link{lamp_read_stop_counts}()},
\code{\link{lamp_s60_legislation}()},
\code{\link{lamp_version_diff}()}
}
\concept{ingest}
