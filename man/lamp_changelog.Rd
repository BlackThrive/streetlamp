% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit-coverage.R
\name{lamp_changelog}
\alias{lamp_changelog}
\title{The data.police.uk changelog as a table of refresh and gap notes}
\usage{
lamp_changelog(refresh = FALSE, dir = lamp_cache_dir())
}
\arguments{
\item{refresh}{Fetch the page again even if a cached copy exists.}

\item{dir}{Cache directory; see \code{\link[=lamp_cache_dir]{lamp_cache_dir()}}.}
}
\value{
A tibble with columns \code{entry_month} (the changelog heading, a
Date), \code{force_name}, \code{force_id}, \code{file_type} (\code{street}, \code{outcomes},
\code{stop-and-search}, a semicolon-separated combination, or \code{all}),
\code{action} (\code{refresh} or \code{not_provided}), \code{from}, \code{to} (Dates, \code{NA} when
the entry covers all months) and \code{text} (the entry as published).
Entries that do not follow the refresh or gap patterns are kept with
\code{action = NA}.
}
\description{
Fetches and parses the publisher's changelog, which records when a force
re-supplied months ("data refresh") or failed to supply a month ("not
provided"). \code{\link[=lamp_coverage]{lamp_coverage()}} and \code{\link[=lamp_panel]{lamp_panel()}} use it to mark
force-months as \code{refreshed} and to annotate gaps. The page is fetched once
and cached; pass \code{refresh = TRUE} to fetch it again.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
# Requires network access
cl <- lamp_changelog()
cl[cl$force_id == "west-yorkshire", ]
\dontshow{\}) # examplesIf}
}
\seealso{
Other panel: 
\code{\link{lamp_adjacency}()},
\code{\link{lamp_adjacency_from_nb}()},
\code{\link{lamp_boundaries}()},
\code{\link{lamp_coverage}()},
\code{\link{lamp_panel}()},
\code{\link{lamp_population}()},
\code{\link{lamp_revintage}()}
}
\concept{panel}
