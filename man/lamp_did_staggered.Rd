% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/est-staggered.R
\name{lamp_did_staggered}
\alias{lamp_did_staggered}
\title{Difference-in-differences with staggered adoption}
\usage{
lamp_did_staggered(
  panel,
  outcome = "crime_total",
  treatment,
  estimator = c("callaway_santanna", "sun_abraham", "imputation"),
  control_group = c("never_treated", "not_yet_treated"),
  cluster = c("area", "force"),
  family = c("ols_log", "ols_ihs", "identity"),
  window = c(-12, 12)
)
}
\arguments{
\item{panel}{A \code{lamp_panel} from \code{\link[=lamp_panel]{lamp_panel()}}.}

\item{outcome}{The outcome column, for example \code{"crime_total"} or a crime
type key from \code{\link[=lamp_crime_types]{lamp_crime_types()}}.}

\item{treatment}{A \code{\link[=lamp_treatment]{lamp_treatment()}} object of type \code{staggered} (or
\code{event}, which is a single cohort).}

\item{estimator}{\code{"callaway_santanna"}, \code{"sun_abraham"} or
\code{"imputation"}.}

\item{control_group}{\code{"never_treated"} (default) or \code{"not_yet_treated"}.}

\item{cluster}{\code{"area"} (default) or \code{"force"}.}

\item{family}{Scale for the outcome: \code{"ols_log"} (default, log of one plus
the outcome), \code{"ols_ihs"} or \code{"identity"}.}

\item{window}{Relative months to report in the dynamic aggregation.}
}
\value{
A \code{lamp_estimate} of class \code{lamp_did_staggered}. \code{coefficients}
holds the dynamic (event-study) aggregation with a \code{rel_time} column;
\code{diagnostics$overall} is the single summary effect with its standard
error; \code{diagnostics$group_time} holds the group-time effects for the
Callaway and Sant'Anna backend; and \code{diagnostics$pretrend_p} is that
backend's pre-test.
}
\description{
Estimates the effect of an intervention that different areas adopt at
different dates, using an estimator that compares each adopting cohort with
areas that have not yet adopted rather than with areas already treated.
Plain two-way fixed effects does the latter and can return an estimate
outside the range of every area's true effect; this function exists so that
staggered designs are not analysed that way.
}
\section{Identifying assumption}{

Parallel trends holds for each adopting cohort against the chosen control
group, from the period before that cohort adopts onward, and there is no
anticipation: the outcome does not move before adoption in response to it.
With \code{control_group = "never_treated"} the comparison is against areas that
never adopt, which must exist; with \code{"not_yet_treated"} it is against areas
that adopt later, which uses more data but assumes their later adoption
carries no information about their current path.
}

\section{Backends}{

\itemize{
\item \code{callaway_santanna} (default) calls \code{did::att_gt()} and
\code{did::aggte()}, giving one effect per cohort and period and the
aggregations of them.
\item \code{sun_abraham} uses \code{fixest::sunab()}, an interaction-weighted estimator
fitted in one regression.
\item \code{imputation} uses the \code{didimputation} package, which fits the untreated
potential outcome and imputes: efficient, but it needs that package,
which is in Suggests.
}

Counts are modelled on a transformed scale by all three backends, which is
why the function warns when a count outcome meets \code{ols_log} or \code{ols_ihs}:
the coefficients are effects on that scale.
}

\examples{
# small: most of this example's cost is loading the did package, which
# whichever example reaches it first has to pay
sim <- lamp_simulate(n_areas = 24, n_months = 18, design = "staggered", effect = -0.3, seed = 1)
ad <- attr(sim, "truth")$adoption
tr <- lamp_treatment(sim, "staggered", adoption = ad[!is.na(ad$adoption_month), ])
fit <- lamp_did_staggered(sim, "crime_total", tr)
fit
fit$diagnostics$overall
}
\references{
Callaway, B. and Sant'Anna, P. H. C. (2021). Difference-in-differences with
multiple time periods. Journal of Econometrics 225(2), 200-230.

Sun, L. and Abraham, S. (2021). Estimating dynamic treatment effects in
event studies with heterogeneous treatment effects. Journal of Econometrics
225(2), 175-199.

Borusyak, K., Jaravel, X. and Spiess, J. (2024). Revisiting event-study
designs: robust and efficient estimation. Review of Economic Studies 91(6).
}
\seealso{
Other estimators: 
\code{\link{lamp_allocation}()},
\code{\link{lamp_crimes_prevented}()},
\code{\link{lamp_displacement_quotient}()},
\code{\link{lamp_elasticity}()},
\code{\link{lamp_event_study}()},
\code{\link{lamp_spillover}()},
\code{\link{lamp_synth}()},
\code{\link{lamp_twfe}()},
\code{\link{tidy.lamp_estimate}()}
}
\concept{estimators}
