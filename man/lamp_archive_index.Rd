% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest-archive.R
\name{lamp_archive_index}
\alias{lamp_archive_index}
\title{Index of the data.police.uk archive}
\usage{
lamp_archive_index(refresh = FALSE, dir = lamp_cache_dir())
}
\arguments{
\item{refresh}{Fetch the page again even if a cached copy exists.}

\item{dir}{Cache directory; see \code{\link[=lamp_cache_dir]{lamp_cache_dir()}}.}
}
\value{
A tibble of class \code{lamp_archive_index} with one row per snapshot:
\code{archive} (identifier such as \code{"2026-07"}), \code{label}, \code{url}, \code{from} and
\code{to} (first and last data month in the zip), \code{n_months}, \code{size_gb} (as
shown on the page) and \code{md5} (the published checksum of the zip). The
attribute \code{fetched_at} records when the page was read.
}
\description{
Lists the monthly archive snapshots published at
\url{https://data.police.uk/data/archive/}: one zip per month, each holding the
street, outcomes and stop-and-search CSV files for every force and, since
the May 2017 snapshot, a rolling window of the latest 36 months (earlier
snapshots hold every month back to December 2010). The index is fetched
once and cached; pass \code{refresh = TRUE} to fetch it again. Without network
access a cached index is returned with a warning, and an error of class
\code{streetlamp_error_network} is raised if there is no cache.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
# Requires network access
idx <- lamp_archive_index()
idx[idx$archive >= "2026-01", ]
\dontshow{\}) # examplesIf}
}
\seealso{
Other ingest: 
\code{\link{lamp_archive_download}()},
\code{\link{lamp_archive_register}()},
\code{\link{lamp_archive_snapshot}()},
\code{\link{lamp_list_versions}()},
\code{\link{lamp_lsoa_vintage}()},
\code{\link{lamp_read_crime}()},
\code{\link{lamp_read_outcomes}()},
\code{\link{lamp_read_stop_counts}()},
\code{\link{lamp_s60_legislation}()},
\code{\link{lamp_select_version}()},
\code{\link{lamp_version_diff}()}
}
\concept{ingest}
