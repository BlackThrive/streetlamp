% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diag-pretrends.R
\name{lamp_pretrends}
\alias{lamp_pretrends}
\title{Test and interpret pre-trends}
\usage{
lamp_pretrends(estimate, power = c(0.5, 0.8), level = 0.05)
}
\arguments{
\item{estimate}{A \code{lamp_estimate} from \code{\link[=lamp_event_study]{lamp_event_study()}}.}

\item{power}{Detection probabilities to report, default 0.5 and 0.8.}

\item{level}{Significance level of the pre-trend test, default 0.05.}
}
\value{
A list of class \code{lamp_pretrends} with \code{test} (statistic, degrees
of freedom and p value), \code{coefficients} (the pre-period coefficients),
\code{power} (a tibble of \code{power}, \code{slope} in log points per month, and
\code{bias_mean_post}, the bias such a slope would add to the mean
post-period coefficient) and \code{interpretation}.
}
\description{
Reports the joint test that every pre-period coefficient of an event study
is zero, and answers the question the test cannot: how large a pre-trend
could have been present without this test detecting it, and how much bias
that undetected pre-trend would put into the post-period estimates.
}
\details{
The power calculation follows the argument in Roth (2022) and is
implemented here rather than taken from another package. Under a linear
violation of parallel trends with slope \code{s} per month, the expected
pre-period coefficients are \code{s} times their relative time (measured from
the reference month), so the Wald statistic is non-central chi-square with
non-centrality \verb{s^2 * t' V^-1 t}, where \code{t} is that vector of relative
times and \code{V} the clustered covariance of the pre-period coefficients.
Inverting this gives the slope the test detects with a given probability.
The bias column shows what that slope would add to each post-period
coefficient if it continued, which is the quantity that matters: a test
that passes while remaining blind to a trend big enough to explain the
result is not reassurance.
}
\examples{
sim <- lamp_simulate(n_areas = 24, n_months = 24, design = "event", effect = -0.25, seed = 5)
truth <- attr(sim, "truth")
tr <- lamp_treatment(
  sim, "event",
  date = truth$event_date, scope = truth$treated_areas, window = c(-6, 6)
)
es <- lamp_event_study(sim, "crime_total", tr, window = c(-6, 6))
pt <- lamp_pretrends(es)
pt
}
\references{
Roth, J. (2022). Pretest with caution: event-study estimates
after testing for parallel trends. American Economic Review: Insights
4(3), 305-322.
}
\seealso{
Other diagnostics: 
\code{\link{lamp_placebo}()}
}
\concept{diagnostics}
