% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geography-boundaries.R
\name{lamp_boundaries}
\alias{lamp_boundaries}
\title{ONS digital boundaries as an sf layer}
\usage{
lamp_boundaries(
  type = c("lsoa21", "lsoa11", "msoa21", "lad", "pfa"),
  dir = lamp_cache_dir(),
  refresh = FALSE
)
}
\arguments{
\item{type}{One of \code{"lsoa21"} (default), \code{"lsoa11"}, \code{"msoa21"}, \code{"lad"}
or \code{"pfa"}.}

\item{dir}{Cache directory; see \code{\link[=lamp_cache_dir]{lamp_cache_dir()}}.}

\item{refresh}{Fetch again even if a cached copy exists.}
}
\value{
An \code{sf} object with columns \code{area} (code), \code{name} and \code{geometry}
(multipolygons, EPSG:27700), sorted by \code{area}, with attributes \code{vintage}
(for example \code{"lsoa21-bgc-v5"}), \code{product} (the ONS title and item
identifier) and \code{retrieved}.
}
\description{
Fetches the generalised, clipped (BGC, 20 m) boundaries for a geography
from the ONS Open Geography Portal, in British National Grid (EPSG:27700),
and caches them. Products: 2021 LSOAs, 2011 LSOAs, 2021 MSOAs, December
2022 local authority districts (England and Wales only) and December 2023
police force areas. The generalised boundaries suit adjacency and mapping;
a search made within a few metres of a boundary can be assigned to the
neighbouring area, so supply full-resolution boundaries yourself for
point-in-polygon work that needs that precision.
}
\details{
Without network access a cached copy is returned, and an error of class
\code{streetlamp_error_network} is raised if there is none. A first fetch of
the 2021 LSOA layer reads about 50 MB in 18 pages.

Source: Office for National Statistics licensed under the Open Government
Licence v3.0; contains OS data, Crown copyright and database right 2024.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
# Requires network access on first use
pfa <- lamp_boundaries("pfa")
plot(sf::st_geometry(pfa))
adj <- lamp_adjacency(pfa)
\dontshow{\}) # examplesIf}
}
\seealso{
Other panel: 
\code{\link{lamp_adjacency}()},
\code{\link{lamp_adjacency_from_nb}()},
\code{\link{lamp_changelog}()},
\code{\link{lamp_coverage}()},
\code{\link{lamp_panel}()},
\code{\link{lamp_population}()},
\code{\link{lamp_revintage}()}
}
\concept{panel}
