% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geography-areas.R
\name{lamp_adjacency}
\alias{lamp_adjacency}
\title{Spatial adjacency for a set of areas}
\usage{
lamp_adjacency(boundaries, method = c("queen", "rook", "knn"), k = 6L)
}
\arguments{
\item{boundaries}{An \code{sf} polygon layer with an \code{area} column, for example
from \code{lamp_boundaries()}.}

\item{method}{\code{"queen"} (default), \code{"rook"} or \code{"knn"}.}

\item{k}{Number of neighbours for \code{method = "knn"}.}
}
\value{
A list of class \code{lamp_adjacency} with \code{areas} (identifiers in
order), \code{nb} (an \code{spdep} neighbour list), \code{listw} (row-standardised
weights, \code{style = "W"}), \code{method}, \code{k}, \code{n_islands}, \code{boundary_vintage}
(taken from the \code{vintage} attribute of \code{boundaries} when present) and
\code{created}.
}
\description{
Builds a neighbour list and row-standardised spatial weights for area
polygons with \code{spdep}, for the spillover estimators and for Moran's I
diagnostics. Queen contiguity (any shared point) is the default; rook
contiguity needs a shared edge; \code{knn} links each area to its \code{k} nearest
neighbours by centroid and symmetrises the result. Areas with no
neighbours (islands) are allowed and counted.
}
\examples{
squares <- sf::st_sf(
  area = c("a", "b", "c"),
  geometry = sf::st_sfc(
    sf::st_polygon(list(rbind(c(0, 0), c(1, 0), c(1, 1), c(0, 1), c(0, 0)))),
    sf::st_polygon(list(rbind(c(1, 0), c(2, 0), c(2, 1), c(1, 1), c(1, 0)))),
    sf::st_polygon(list(rbind(c(5, 5), c(6, 5), c(6, 6), c(5, 6), c(5, 5)))),
    crs = 27700
  )
)
adj <- lamp_adjacency(squares)
adj
adj$nb
}
\seealso{
Other panel: 
\code{\link{lamp_adjacency_from_nb}()},
\code{\link{lamp_boundaries}()},
\code{\link{lamp_changelog}()},
\code{\link{lamp_coverage}()},
\code{\link{lamp_panel}()},
\code{\link{lamp_population}()},
\code{\link{lamp_revintage}()}
}
\concept{panel}
