% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{lamp_colours}
\alias{lamp_colours}
\title{The colours streetlamp draws with}
\usage{
lamp_colours()
}
\value{
A named character vector of hex colours. \code{series} is the main data
colour, \code{contrast} the second series where a plot has two, \code{accent} the
colour for the one mark the reader is meant to compare against (the
actual estimate in a placebo distribution), and \code{neutral} the fill for
marks that carry no identity. The \code{ink}, \code{secondary}, \code{muted}, \code{grid},
\code{baseline}, \code{surface} and \code{shade} entries are the text and chrome
colours. The remaining entries are named for coverage statuses.
}
\description{
A named vector of the colours every \code{plot()} method in the package uses,
so that figures drawn outside the package can match them. The series
colours were chosen and checked as a set for separation under the common
forms of colour vision deficiency; the coverage statuses are drawn from
the same set so that a status never impersonates a series.
}
\examples{
lamp_colours()[c("series", "contrast", "accent")]
}
\seealso{
Other presentation: 
\code{\link{lamp_table}()},
\code{\link{lamp_theme}()}
}
\concept{presentation}
