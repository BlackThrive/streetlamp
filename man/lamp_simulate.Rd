% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{lamp_simulate}
\alias{lamp_simulate}
\title{Simulate a panel with a known treatment effect}
\usage{
lamp_simulate(
  n_areas = 36L,
  n_months = 24L,
  design = c("staggered", "event", "continuous", "spillover"),
  effect = -0.15,
  spillover_effect = 0.05,
  adoption = NULL,
  missing = 0,
  base_rate = 8,
  population = 1500,
  seed = NULL
)
}
\arguments{
\item{n_areas}{Number of areas.}

\item{n_months}{Number of months.}

\item{design}{One of \code{"staggered"}, \code{"event"}, \code{"continuous"} or
\code{"spillover"}.}

\item{effect}{True treatment effect in log points on the affected crime
types (for \code{continuous}, the elasticity of crime with respect to
\code{log(stops + 1)}).}

\item{spillover_effect}{True neighbour effect in log points, used by the
\code{spillover} design.}

\item{adoption}{Adoption months for the \code{staggered} design, as month
indices within the panel; \code{NULL} picks three evenly spaced cohorts.}

\item{missing}{Share of force-months with no street file (0 to 1).}

\item{base_rate}{Expected monthly count per area before fixed effects.}

\item{population}{Population per area, used for \code{stop_rate}.}

\item{seed}{Random seed.}
}
\value{
A \code{lamp_panel} with the usual columns plus \code{treat} (the treatment
variable) and, for the event and staggered designs, \code{rel_time} and
\code{cohort}. Its contract carries the treatment definition, and
\code{attr(x, "truth")} holds the true \code{effect}, the implied effect on the
crime total (\code{effect_crime_total}, smaller in size than \code{effect}
because bicycle theft is unaffected), \code{spillover_effect}, \code{design}, the
affected crime types, the treated areas, the event date, the adoption
table and the neighbour list.
}
\description{
Generates an area-by-month panel from a known data-generating process, for
testing estimators and for the simulation evidence in the vignettes. Crime
counts are Poisson draws around area and month fixed effects, and the
treatment multiplies the mean of the affected crime types by \code{exp(effect)},
so \code{effect} is a log point effect: -0.1 is a 9.5 percent reduction.
}
\details{
Designs:
\itemize{
\item \code{event}: one date; the areas in the treated half are treated from that
month onward. Suits \code{\link[=lamp_event_study]{lamp_event_study()}}.
\item \code{staggered}: areas adopt in cohorts spread across the middle of the
period, with never-treated areas kept as controls. Plain two-way fixed
effects are biased here, which is what \code{lamp_did_staggered()} is for.
\item \code{continuous}: every area has a stop intensity that varies over time, and
the outcome responds to \code{log(stops + 1)} with elasticity \code{effect}.
\item \code{spillover}: as \code{event}, and in addition the mean treatment of an area's
first-order lattice neighbours shifts its outcome by \code{spillover_effect},
so an estimator that omits the neighbour term is biased.
}

Areas sit on a square lattice and belong to two forces, so that
force-month missingness, clustering and adjacency are all exercisable. A
share \code{missing} of force-months has no street file: those rows carry \code{NA}
counts and \code{coverage_status = "missing"}, exactly as a real panel does.
Bicycle theft is never affected by treatment, which makes it the default
placebo outcome in \code{\link[=lamp_placebo]{lamp_placebo()}}.
}
\examples{
sim <- lamp_simulate(n_areas = 36, n_months = 24, design = "event", effect = -0.2)
sim
attr(sim, "truth")$effect
table(sim$coverage_status)
}
\concept{simulation}
