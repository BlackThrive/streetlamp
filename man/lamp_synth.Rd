% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/est-synth.R
\name{lamp_synth}
\alias{lamp_synth}
\title{Synthetic control for a single treated area or force}
\usage{
lamp_synth(
  panel,
  outcome = "crime_total",
  treated_unit,
  treatment_start,
  donors = NULL,
  method = c("internal", "synth", "gsynth"),
  placebo = TRUE,
  min_pre = 6L
)
}
\arguments{
\item{panel}{A \code{lamp_panel}.}

\item{outcome}{The outcome column.}

\item{treated_unit}{The area that was treated.}

\item{treatment_start}{The first treated month.}

\item{donors}{Candidate donor areas; \code{NULL} uses every other area that has
a complete series.}

\item{method}{\code{"internal"} (default), \code{"synth"} or \code{"gsynth"}.}

\item{placebo}{Run in-space placebos over the donors for the permutation
p value.}

\item{min_pre}{Months of pre-period required.}
}
\value{
A list of class \code{lamp_synth} with \code{weights}, \code{path} (a tibble of
month, observed, synthetic and gap), \code{effect} (mean post-period gap),
\code{rmspe_pre}, \code{rmspe_post}, \code{rmspe_ratio}, \code{p_value} (the share of donors
with a ratio at least as large), \code{placebos} and \code{assumption}.
}
\description{
Builds a weighted average of untreated donor units whose pre-period path
matches the treated unit's, and reads the gap between them afterwards as
the effect. Inference is by permutation: the same procedure is run
pretending each donor was treated, and the treated unit's post-period fit
is compared with that distribution.
}
\section{Identifying assumption}{

The weighted donors reproduce what the treated unit would have done
without the intervention. This is credible only when the pre-period fit is
close over a long window, no donor was itself affected by the
intervention, and nothing else happened to the treated unit at the same
time. A poor pre-period fit invalidates the comparison, which is why the
pre-period root mean squared error is reported first.
}

\examples{
sim <- lamp_simulate(n_areas = 20, n_months = 30, design = "event", effect = -0.4, seed = 1)
truth <- attr(sim, "truth")
sc <- lamp_synth(
  sim, "crime_total",
  treated_unit = truth$treated_areas[1],
  treatment_start = truth$event_date,
  donors = setdiff(unique(sim$area), truth$treated_areas),
  placebo = FALSE
)
sc
}
\references{
Abadie, A., Diamond, A. and Hainmueller, J. (2010). Synthetic control
methods for comparative case studies. Journal of the American Statistical
Association 105(490), 493-505.

Xu, Y. (2017). Generalized synthetic control method. Political Analysis
25(1), 57-76.
}
\seealso{
Other estimators: 
\code{\link{lamp_allocation}()},
\code{\link{lamp_crimes_prevented}()},
\code{\link{lamp_did_staggered}()},
\code{\link{lamp_displacement_quotient}()},
\code{\link{lamp_elasticity}()},
\code{\link{lamp_event_study}()},
\code{\link{lamp_spillover}()},
\code{\link{lamp_twfe}()},
\code{\link{tidy.lamp_estimate}()}
}
\concept{estimators}
