% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classifications.R
\name{lamp_crime_types}
\alias{lamp_crime_types}
\title{Crime type classification used by data.police.uk}
\usage{
lamp_crime_types()
}
\value{
A tibble with one row per category and columns \code{crime_type},
\code{key}, \code{api_slug}, \code{is_asb}, \code{in_crime_total}, \code{group} and \code{broad_group}.
}
\description{
data.police.uk assigns every record in the street-level files to one of
fourteen categories: anti-social behaviour plus thirteen crime types. This
table gives the labels exactly as they appear in the \verb{Crime type} column of
the archive CSV files, a snake_case key used for panel columns, the slug used
by the police.uk API, and two package-defined groupings.
}
\details{
Anti-social behaviour (ASB) is not a crime: ASB incidents carry no Crime ID
and no outcome, so \code{is_asb} is \code{TRUE} and \code{in_crime_total} is \code{FALSE} for
that row and \code{lamp_panel()} keeps ASB in its own series.

The category set was verified against the police.uk API on 2026-09-16 and
has been stable since June 2013, when data.police.uk introduced bicycle
theft and theft from the person (previously within other theft), split
public disorder and weapons into possession of weapons and public order,
and renamed violent crime to violence and sexual offences. Earlier archive
files therefore use a smaller legacy set; \code{lamp_read_crime()} maps legacy
labels where the mapping is one to one and flags the rest.

\code{group} follows the Home Office offence groups with the six theft
categories combined; \code{broad_group} collapses further into \code{violent},
\code{acquisitive}, \code{drugs}, \code{damage}, \code{other} and \code{asb}. Both groupings are the
package's own and are documented here so that results by offence group can
be reproduced.
}
\examples{
lamp_crime_types()
}
\seealso{
\code{\link[=lamp_outcome_types]{lamp_outcome_types()}}

Other bundled data: 
\code{\link{lamp_bank_holidays}()},
\code{\link{lamp_lsoa_lookup}()},
\code{\link{lamp_outcome_types}()},
\code{\link{lamp_shocks}()}
}
\concept{bundled data}
