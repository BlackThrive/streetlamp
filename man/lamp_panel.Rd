% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel-build.R
\name{lamp_panel}
\alias{lamp_panel}
\title{Build an area-by-month panel of crime and stops}
\usage{
lamp_panel(
  crime,
  outcomes = NULL,
  stops = NULL,
  area = c("lsoa21", "lsoa11", "msoa21", "lad", "pfa"),
  population = NULL,
  covariates = NULL,
  include_asb = FALSE,
  attribution = c("falls_within", "reported_by"),
  areas = NULL,
  adjacency = NULL,
  changelog = NULL
)
}
\arguments{
\item{crime}{Records from \code{\link[=lamp_read_crime]{lamp_read_crime()}}.}

\item{outcomes}{Optional records from \code{\link[=lamp_read_outcomes]{lamp_read_outcomes()}}; adds
\code{outcomes_total} and one column per outcome group, counted by the month
the outcome was recorded.}

\item{stops}{Optional stop counts from \code{\link[=lamp_read_stop_counts]{lamp_read_stop_counts()}} at the
panel's area level (or at \code{lsoa21} level, which is aggregated), or a
user-supplied tibble with columns \code{area}, \code{month}, \code{stops} and
optionally \code{stops_s60} and \code{coverage_status}, which is tagged
\code{user_supplied} in the contract and never zero-filled.}

\item{area}{The area level: \code{"lsoa21"} (default), \code{"lsoa11"}, \code{"msoa21"},
\code{"lad"} (2022 local authority districts) or \code{"pfa"} (police force
areas).}

\item{population}{Optional population by area: the output of
\code{lamp_population()} or a tibble with columns \code{area} and \code{population}.
Needed for \code{stop_rate} (stops per 1,000 residents).}

\item{covariates}{Optional tibble with an \code{area} column (and optionally
\code{month}) whose other columns are joined to the panel.}

\item{include_asb}{Include anti-social behaviour in \code{crime_total}?}

\item{attribution}{Which column names the force a record belongs to:
\code{"falls_within"} (default) or \code{"reported_by"}. Areas are always placed by
location; attribution only affects the diagnostics on records located
outside their force's area and the \code{pfa} level, where records are counted
by the attributed force.}

\item{areas}{Optional character vector restricting the panel to these
areas (identifiers at the chosen level).}

\item{adjacency}{Optional \code{\link[=lamp_adjacency]{lamp_adjacency()}} object for the panel's areas,
stored in the contract for spatial estimators.}

\item{changelog}{Optional \code{\link[=lamp_changelog]{lamp_changelog()}} table used to mark
force-months that the publisher's changelog says were refreshed.}
}
\value{
A tibble of class \code{lamp_panel} with columns \code{area}, \code{month},
\code{force_id}, one column per crime type, \code{crime_total}, \code{asb}, \code{stops},
\code{stops_s60}, \code{population}, \code{stop_rate}, \code{coverage_status},
\code{stops_status}, any outcome and covariate columns, and a contract
retrievable with \code{\link[=lamp_contract]{lamp_contract()}}.
}
\description{
Turns crime records into a balanced area-by-month panel with one column per
crime type, a crime total, an anti-social behaviour series, stop counts,
population and a stop rate, and attaches a full panel contract. The panel
covers every area in the police force areas of the forces present in
\code{crime} (or the areas you name) and every month in the records.
}
\details{
Records are placed by their LSOA code. Files published with 2011 codes are
re-vintaged to 2021 with the bundled ONS lookup (or 2021 to 2011 when
\code{area = "lsoa11"}), and the contract's \code{geography$revintage} element
records how many records and areas were unchanged, split, merged or
complex. Coarser areas (\code{msoa21}, \code{lad}, \code{pfa}) are reached through
\code{\link[=lamp_area_lookup]{lamp_area_lookup()}}.

"Force did not submit" is never a zero: counts are zero-filled only in
force-months whose street file was submitted; force-months with no file
are \code{NA} and carry \code{coverage_status = "missing"}. Force-months whose record
count is suspiciously low are kept but flagged \code{partial_suspected}; see
\code{\link[=lamp_coverage]{lamp_coverage()}}.

Anti-social behaviour is not a crime: it goes to the \code{asb} column and is
excluded from \code{crime_total} unless \code{include_asb = TRUE}. The crime type
columns are the snake_case keys of \code{\link[=lamp_crime_types]{lamp_crime_types()}}; the legacy
\code{public_disorder_and_weapons} column appears only when records from before
May 2013 are present.
}
\examples{
cache <- tempfile("streetlamp-cache-")
zip <- system.file("extdata", "archive", "2026-07.zip", package = "streetlamp")
lamp_archive_register(zip, dir = cache)
crime <- lamp_read_crime(cache, forces = "dyfed-powys", months = "2026-07")
panel <- lamp_panel(crime, area = "lad")
panel
lamp_contract(panel)$coverage

# The bundled sample is a full two-force LSOA panel over 24 months
lamp_sample_panel()
}
\seealso{
Other panel: 
\code{\link{lamp_adjacency}()},
\code{\link{lamp_adjacency_from_nb}()},
\code{\link{lamp_boundaries}()},
\code{\link{lamp_changelog}()},
\code{\link{lamp_coverage}()},
\code{\link{lamp_population}()},
\code{\link{lamp_revintage}()}
}
\concept{panel}
